# Webrtc-Group-Chat-ScreenShare-PeerJS

Webrtc conference, group chat with screen share using peerjs

# How to Use

- Clone or download zip file and extract
- Install packages `npm install`
- Run: `npm run dev` or `npm run start`
